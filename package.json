{
  "name": "nila-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build:css": "postcss src/styles.css -o src/styles.build.css",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.0.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemailer": "^8.0.7"
  }
}
